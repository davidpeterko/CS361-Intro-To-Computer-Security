public class BadInstruction{
	
	String status;

	public BadInstruction(String badstatus){

		status = badstatus;
		//do stuff for bad instruction?
	}

	//Note: we can check if it is an instance of a BadInstruction object 
	//by checking if(ob.getClass() == BadInstruction.class)

}